# ---- FlowSight local artifacts (do not commit) ----
docs/import/**
!.gitkeep
!docs/import/.gitkeep
# -----------------------------------------------

# FS_LOCAL_IGNORE_START
# Local artifacts / exports / backups
.local_artifacts/
*.bak
*.bak_*
*.bak.*
*.zip

# If Webflow exports create temp dirs in-repo:
customers/template/
# FS_LOCAL_IGNORE_END

# FlowSight: local-only artifacts (do not commit)
backups/
intern/
handoff_bindings_audit_full.txt
docs/master/Archiv/
core/*.bak*
# Local artifacts (non-SSOT)
_backup/
*.webflow.zip
sanitar-template.webflow.zip
